/*
1. Neutral/Base Color:

  - Page Background.
  - Primary Section Background: used for cards, cart, order, summary, checkout, and modals.
  - secondary Section Background: header, footer, sidebar, forms.

2. Primary Accent Color:

    - primary buttons (CTA's button e.g. shop now, add to cart, order, and checkout buttons).
    
3. Secondary Accent Color:

    - secondary buttons( authentication buttons) and secondary container(badge, notifications)

3. Tertiary Accent Color:

    - variant color(focus-outline(boder-coloring, disabled state coloring, active, hover)) and tertiary buttons

4. Text / Article Color:

 Typography Scale and Coloring.

   H1 (main heading): black. sm(16px), md(20px), lg(24px), xl(28px) and xxl(32px). 
   H2 (subHading): black. sm(14px), md(18px), lg(22px), xl(26px) and xxl(30px). for supporting text(input labels, meta data)
   H3 (subtle text): light color e.g. placeholder, helper text(don't you have an account ?), default value text. sm(12px), md(16px), xl(20px), xxl(24px)
   p (text or body): black. e.g. regular text sm(10px), md(14px), lg(18px), xxl(22px)

 ⭕ Border Radius Scale:
 xs --> tertiary buttons
 sm --> secondary buttons
 md --> Primary buttons and inputs
 lg --> primary sections and cards
 xl --> hero sections
 full --> avatar
 🌑 Shadow Scale:
 xs
 sm
 lg
 md
 xl
 📐 Spacing Scale( Padding && Margin and gap(for flex and grid))
 xs- icons's container padding.
 sm: buttons's padding.
 md: form and card padding.
*/